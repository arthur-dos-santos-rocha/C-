#include <stdio.h>
int main()
{
   //criar calculador
   float a, b, resultado;
   int opcao;
   
   printf("Digite o primeiro numero:");
   scanf("%f",&a);
   printf("Digite o segundo valor:");
   scanf("%f",&b);
   
   
   printf(" \n1-soma\n2-subtração\n3-multiplicação\n4-Divisão\n");
   scanf("%d", &opcao);
   
   if (opcao == 1 ){
       resultado = a + b; 
   }else if (opcao == 2){
       resultado = a - b;
   }else if (opcao == 3){
       resultado = a * b;
   }else if(opcao == 4){
   if(&b != 0){
    printf("Não é possivel ser dividido por zero\n");
    }
       resultado = a/ b;
   }else{
       printf("Não é possivel continuar");
   }
   
   printf("O resultado é %f", resultado);
    return 0;
}
